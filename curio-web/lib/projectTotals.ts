/**
 * Project total step counts for admin progress display.
 * Re-exports from lesson-core — computed dynamically from lesson step data,
 * using the same counting logic as CodeLessonBase (excludes optional lessons and steps).
 */
export { getProjectTotalSteps } from "@/src/lesson-core/projectStepRegistry";
